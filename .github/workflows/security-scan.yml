name: Security Scan (consolidated into Android CI)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Security scanning is consolidated into .github/workflows/android-ci.yml'
