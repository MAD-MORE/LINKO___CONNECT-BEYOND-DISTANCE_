name: Relay CI (consolidated into Android CI)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Relay CI is consolidated into .github/workflows/android-ci.yml'
