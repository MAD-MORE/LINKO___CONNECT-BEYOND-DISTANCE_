name: Deploy Relay (consolidated into Android CI)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Relay deployment is consolidated into .github/workflows/android-ci.yml'
