package com.linkshare.app.data

object SignalingConfig {
    /** Set this to the deployed HTTPS LINKO signaling URL before release. */
    const val BASE_URL: String = "https://REPLACE_WITH_LINKO_SIGNALING_HOST"
}