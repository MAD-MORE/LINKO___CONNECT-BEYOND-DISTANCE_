<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- DownloadManager stores LINKO APK updates under getExternalFilesDir(UPDATE_DIRECTORY). -->
    <!-- Expose only LINKO's app-specific external files area; never the shared external root. -->
    <external-files-path name="linko_updates" path="." />
</paths>
