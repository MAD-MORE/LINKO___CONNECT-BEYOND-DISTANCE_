<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- LINKO must never run as a fullscreen window: Android's status and
         navigation surfaces remain outside the app content on Android 8+. -->
    <style name="Theme.LinkShare" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:fontFamily">monospace</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
