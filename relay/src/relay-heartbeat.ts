import { request } from "node:https";
/** Relay -> Supabase liveness publisher. */
export interface RelayHeartbeatOptions { nodeId:string; host:string; port:number; region:string; heartbeatUrl:string; registrationToken:string; intervalMs?:number; maxSessions?:number; currentSessions?:()=>number; }
export class RelayHeartbeat { private timer:NodeJS.Timeout|null=null; constructor(private readonly o:RelayHeartbeatOptions){} start(){if(this.timer)return; void this.send(); this.timer=setInterval(()=>void this.send(),this.o.intervalMs??15000); this.timer.unref();} stop(){if(this.timer){clearInterval(this.timer);this.timer=null;}} private async send(){const body=JSON.stringify({nodeId:this.o.nodeId,host:this.o.host,port:this.o.port,region:this.o.region,currentSessions:this.o.currentSessions?.()??0,maxSessions:this.o.maxSessions??1000}); const u=new URL(this.o.heartbeatUrl); await new Promise<void>(resolve=>{const r=request({hostname:u.hostname,port:u.port||443,path:u.pathname+u.search,method:"POST",headers:{"content-type":"application/json","content-length":Buffer.byteLength(body),"x-linko-relay-token":this.o.registrationToken},timeout:5000},res=>{res.resume();resolve();}); r.on("timeout",()=>r.destroy(new Error("heartbeat_timeout"))); r.on("error",()=>resolve()); r.write(body); r.end();});}}
export function createRelayHeartbeatFromEnv(currentSessions?:()=>number):RelayHeartbeat|null {const token=process.env.LINKO_RELAY_REGISTRATION_TOKEN; const url=process.env.LINKO_RELAY_HEARTBEAT_URL; if(!token||!url)return null; return new RelayHeartbeat({nodeId:process.env.RELAY_NODE_ID??"relay-1",host:process.env.RELAY_PUBLIC_HOST??"linko-relay.fly.dev",port:Number(process.env.UDP_PORT??7000),region:process.env.RELAY_REGION??"iad",heartbeatUrl:url,registrationToken:token,intervalMs:Number(process.env.RELAY_HEARTBEAT_INTERVAL_MS??15000),maxSessions:Number(process.env.RELAY_MAX_SESSIONS??1000),currentSessions});}
