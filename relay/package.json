{
  "name": "linko-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Linko relay node — forwards encrypted UDP tunnel traffic between Provider and Receiver.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "tsx src/relay-server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/relay-server.js",
    "test": "find src -type f -name '*.test.ts' -print0 | xargs -0 -r node --test --import tsx"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
