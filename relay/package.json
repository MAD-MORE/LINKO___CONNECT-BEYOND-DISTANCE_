{
  "name": "linko-relay",
  "version": "0.1.0",
  "private": true,
  "description": "LINKO encrypted UDP relay",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/relay-server.js",
    "test": "node --test --import tsx src/*.test.ts"
  },
  "dependencies": {
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
