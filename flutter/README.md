# Linko Flutter client

Shared Flutter UI and state layer for the Linko client. The native VPN tunnel
implementation remains platform-specific: Android uses `VpnService`; iOS will
use `NetworkExtension`.
