{
  "name": "linko-control-plane",
  "version": "0.1.0",
  "private": true,
  "description": "LINKO backend control plane: authentication, device registry, sessions and signaling.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "tsx --test src/**/*.test.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
