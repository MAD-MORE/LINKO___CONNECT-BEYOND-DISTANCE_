{
  "name": "linko-control-plane",
  "version": "0.1.0",
  "private": true,
  "description": "LINKO backend control plane: authentication, device registry, sessions and signaling.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "find src -type f -name '*.test.ts' -print0 | xargs -0 -r node --test --import tsx",
    "db:verify": "tsx src/db-verify.ts"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.15.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
